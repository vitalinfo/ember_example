<div>{{#link-to 'dashboard.tasks.new'}}New{{/link-to}}</div>
{{outlet}}
<table>
	{{#each model as |task|}}
		<tr>
			<td>{{input type="checkbox" checked=task.completed change=(action "triggerCompleted" task)}}</td>
			<td>{{task.title}}</td>
			<td>{{task.start_at}}</td>
		</tr>
	{{/each}}
</table>